// lists all the tests and provides customization for tests to run

/*import React from 'react';
import {Tabs, Tab, Container, ListGroup, ListGroupItem,Col,Row,Button} from 'react-bootstrap';
import './styles/styles.scss';
class TestGroup extends React.Component{
  constructor(props,context){
    debugger;
    super(props,context);
    this.state = {
      allTests:props.tests,
      selected:[],
      unselected:props.tests
    }
  }
  runTests(tests){
    console.log('initiated test run with tests:', tests);
  }
  render(){
    debugger;
    /!*const ListGroupUnselected = this.state.unselected.map(elt=>(
      <ListGroup.Item>{elt.name}<small>{elt.path}</small></ListGroup.Item>
    ));*!/
    return(
      <Container>
        <Row>
          <Col>
            <ListGroup>
              {}
            </ListGroup>
          </Col>
          <Col>
            <Button variant="outline-primary" class="list-button"> > </Button>
            <Button variant="outline-primary" class="list-button"> >> </Button>
            <Button variant="outline-primary" class="list-button"> &lt; </Button>
            <Button variant="outline-primary" class="list-button"> &lt;&lt; </Button>
            <Button variant="outline-primary" class="list-button"> Run -> </Button>
          </Col>
          <Col>
            <ListGroup></ListGroup>
          </Col>
        </Row>


    </Container>
    );
  }

}
export default TestGroup;*/
